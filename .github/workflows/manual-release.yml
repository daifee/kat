name: Manual Release

on:
  workflow_dispatch:
    inputs:
      name:
        description: 包名称
        type: string
        required: true
        default: "@daifee"
      version:
        description: 新版本号
        type: string
        required: true

jobs:
  update-package-version:
    name: 更新版本号
    runs-on: ubuntu-latest
    env:
      GIT_TAG: "release-${{ github.event.inputs.name }}@${{ github.event.inputs.version }}"
    outputs:
      git_tag: ${{ steps.define_tag.outputs.git_tag }}
    steps:
      - name: 检出
        uses: actions/checkout@v3
      - name: 定义git tag
        id: define_tag
        run: echo "::set-output name=git_tag::${GIT_TAG}"
      - name: 更新 package.version
        run: |
          make update-package-version name=${{ github.event.inputs.name }} version=${{ github.event.inputs.version }}
          git status
      - name: 提交代码
        run: |
          git config user.name "robot"
          git config user.email "robot@daifee.com"
          git add --all
          git commit -m "${GIT_TAG}"
          git tag ${GIT_TAG}
          git push --all
          git push --tags

  npm-publish:
    name: 发布到NPM
    runs-on: ubuntu-latest
    needs: update-package-version
    steps:
      - name: 检出
        uses: actions/checkout@v3
      - name: publish
        run: |
          git pull
          git log
          make npm-publish tag=${{ needs.update-package-version.outputs.git_tag }}
        env:
          YARN_NPM_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
